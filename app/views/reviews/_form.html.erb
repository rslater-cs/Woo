<%= form_with(model: review, tutorID: session[:tutorID], userID: current_user.id) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Rating: (must be between 0 and 5)" %>
    <br>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <br>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
